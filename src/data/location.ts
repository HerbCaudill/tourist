import type { Location } from "../types"

/** Where the prototype pretends the user is standing. */
export const location: Location = {
  name: "Candlemaker Row",
  area: "Old Town, Edinburgh",
  coordinates: { lat: 55.9469, lon: -3.1906 },
  accuracyMeters: 12,
}
