# Tourist

React and TypeScript app built with Vite, Tailwind CSS, shadcn/ui, and pnpm.

Run `pnpm dev` to develop, `pnpm build` to build, `pnpm test:all` to verify, and `pnpm lint` to lint.

## Task tracking

Run `bd prime` for task tracking context.
